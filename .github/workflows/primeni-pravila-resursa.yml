name: Primeni pravila resursa

on:
  push:
    branches:
      - sync-google-sheet-2026-08-25
    paths:
      - ".github/workflows/primeni-pravila-resursa.yml"

permissions:
  contents: write

jobs:
  primeni:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Primeni pravila korepetitora i Narodnog pozorista
        run: |
          python - <<'PY'
          from pathlib import Path

          p = Path('src/resavac.py')
          t = p.read_text(encoding='utf-8')

          t = t.replace(
              'INFORMATIKA = "Рачунарство и информатика"\nNP_SALA = "NP-сала"\n',
              'INFORMATIKA = "Рачунарство и информатика"\nREPERTOAR_KLASICNOG = "Репертоар класичног балета"\nNP_SALA = "NP-сала"\n',
              1,
          )

          stari = '''    # NP sala ima veoma usko pravilo. Prva verzija je ne koristi; ostalih deset\n    # sala daju dovoljan kapacitet, a izbegavanje NP znatno smanjuje model.\n    return tuple(p for p in prostorije if p.tip is tip and p.oznaka != NP_SALA)\n'''
          novi = '''    if zahtev.predmet == REPERTOAR_KLASICNOG and zahtev.odeljenja[0] in {\n        "III1", "III2", "IV1", "IV2"\n    }:\n        return tuple(p for p in prostorije if p.tip is tip)\n    return tuple(p for p in prostorije if p.tip is tip and p.oznaka != NP_SALA)\n'''
          if stari not in t:
              raise SystemExit('Nije pronadjeno pravilo prostorija')
          t = t.replace(stari, novi, 1)

          marker = '''        if not dozvoljeni:\n            raise ValueError(\n                f"{zahtev.gde}: нема дозвољеног почетка за „{zahtev.predmet}“"\n            )\n'''
          zamena = '''        if zahtev.korepetitor and jedinica.korepeticija:\n            dozvoljeni = tuple(\n                (dan_i, blok_i)\n                for dan_i, blok_i in dozvoljeni\n                if not _nastavnik_nedostupan(\n                    zahtev.korepetitor,\n                    DANI[dan_i],\n                    tuple(blok_i + pomeraj for pomeraj in jedinica.korepeticija),\n                    nedostupnosti,\n                )\n            )\n        if not dozvoljeni:\n            raise ValueError(\n                f"{zahtev.gde}: нема дозвољеног почетка за „{zahtev.predmet}“"\n            )\n'''
          if marker not in t:
              raise SystemExit('Nije pronadjeno filtriranje A')
          t = t.replace(marker, zamena, 1)

          marker_b = '''                vrednosti_starta_b = [\n                    dan_i * KORAK_DANA + blok_i\n                    for dan_i, blok_i in dozvoljeni_b\n                ]\n'''
          zamena_b = '''                if zahtev.korepetitor and jedinica.korepeticija:\n                    dozvoljeni_b = tuple(\n                        (dan_i, blok_i)\n                        for dan_i, blok_i in dozvoljeni_b\n                        if not _nastavnik_nedostupan(\n                            zahtev.korepetitor,\n                            DANI[dan_i],\n                            tuple(\n                                blok_i + pomeraj\n                                for pomeraj in jedinica.korepeticija\n                            ),\n                            nedostupnosti,\n                        )\n                    )\n                if not dozvoljeni_b:\n                    raise ValueError(\n                        f"{zahtev.gde}: нема дозвољеног почетка у недељи B "\n                        f"за „{zahtev.predmet}“"\n                    )\n                vrednosti_starta_b = [\n                    dan_i * KORAK_DANA + blok_i\n                    for dan_i, blok_i in dozvoljeni_b\n                ]\n'''
          if marker_b not in t:
              raise SystemExit('Nije pronadjeno filtriranje B')
          t = t.replace(marker_b, zamena_b, 1)

          marker_init = '''    jedinice_zahteva: dict[int, list[Jedinica]] = defaultdict(list)\n\n    for jedinica in jedinice:\n'''
          zamena_init = '''    jedinice_zahteva: dict[int, list[Jedinica]] = defaultdict(list)\n    np_izbori: dict[str, list[cp_model.BoolVar]] = defaultdict(list)\n\n    for jedinica in jedinice:\n'''
          if marker_init not in t:
              raise SystemExit('Nije pronadjena inicijalizacija NP')
          t = t.replace(marker_init, zamena_init, 1)

          marker_room = '''            koristi = model.new_bool_var(f"{prefiks}_{prostorija.oznaka}")\n            izbor_prostorije[prostorija.oznaka] = koristi\n            po_lokaciji[prostorija.lokacija].append(koristi)\n'''
          zamena_room = '''            koristi = model.new_bool_var(f"{prefiks}_{prostorija.oznaka}")\n            izbor_prostorije[prostorija.oznaka] = koristi\n            po_lokaciji[prostorija.lokacija].append(koristi)\n            if prostorija.oznaka == NP_SALA:\n                np_izbori[zahtev.odeljenja[0]].append(koristi)\n                if jedinica.trajanje != 2:\n                    model.add(koristi == 0)\n                else:\n                    model.add(blok == 10).only_enforce_if(koristi)\n'''
          if marker_room not in t:
              raise SystemExit('Nije pronadjena petlja prostorija')
          t = t.replace(marker_room, zamena_room, 1)

          marker_constraints = '''    for intervali in intervali_nastavnika.values():\n        model.add_no_overlap(intervali)\n'''
          np_constraints = '''    # Narodno pozoriste: pet dvočasa RKB nedeljno u 16:00–17:40.\n    model.add(sum(np_izbori["IV1"]) == 2)\n    model.add(sum(np_izbori["IV2"]) == 2)\n    model.add(sum(np_izbori["III1"]) + sum(np_izbori["III2"]) == 1)\n\n    for intervali in intervali_nastavnika.values():\n        model.add_no_overlap(intervali)\n'''
          if marker_constraints not in t:
              raise SystemExit('Nije pronadjeno mesto za NP ogranicenja')
          t = t.replace(marker_constraints, np_constraints, 1)
          p.write_text(t, encoding='utf-8')

          p = Path('src/proveravac.py')
          t = p.read_text(encoding='utf-8')
          marker_call = '''    _proveri_versku_i_gradjansko(ulaz, casovi, izvestaj)\n    _upozori_na_pauze_nastavnika(casovi, izvestaj)\n'''
          zamena_call = '''    _proveri_versku_i_gradjansko(ulaz, casovi, izvestaj)\n    _proveri_narodno_pozoriste(casovi, izvestaj)\n    _upozori_na_pauze_nastavnika(casovi, izvestaj)\n'''
          if marker_call not in t:
              raise SystemExit('Nije pronadjen poziv provera')
          t = t.replace(marker_call, zamena_call, 1)

          marker_unavail = '''            izvestaj.greske.append(\n                f"{cas.gde}: наставник {cas.nastavnik} није доступан "\n                f"({cas.dan}, блокови {stavka.od_bloka}–{stavka.do_bloka})"\n            )\n\n    return tuple(zahtevi)\n'''
          zamena_unavail = '''            izvestaj.greske.append(\n                f"{cas.gde}: наставник {cas.nastavnik} није доступан "\n                f"({cas.dan}, блокови {stavka.od_bloka}–{stavka.do_bloka})"\n            )\n        if (\n            cas.korepetitor\n            and stavka.nastavnik == cas.korepetitor\n            and stavka.dan == cas.dan\n            and stavka.od_bloka <= cas.blok <= stavka.do_bloka\n        ):\n            izvestaj.greske.append(\n                f"{cas.gde}: корепетитор {cas.korepetitor} није доступан "\n                f"({cas.dan}, блокови {stavka.od_bloka}–{stavka.do_bloka})"\n            )\n\n    return tuple(zahtevi)\n'''
          if marker_unavail not in t:
              raise SystemExit('Nije pronadjena provera nedostupnosti')
          t = t.replace(marker_unavail, zamena_unavail, 1)

          marker_func = '''def _proveri_smenu(\n'''
          nova_func = '''def _proveri_narodno_pozoriste(\n    casovi: Sequence[Cas], izvestaj: Izvestaj\n) -> None:\n    np_casovi = [cas for cas in casovi if cas.prostorija == "NP-сала"]\n    po_odeljenju = Counter(\n        cas.odeljenja[0] for cas in np_casovi if len(cas.odeljenja) == 1\n    )\n    if po_odeljenju["IV1"] != 4:\n        izvestaj.greske.append(\n            f"NP-сала: IV1 мора имати 4 блока (два двочаса), има {po_odeljenju['IV1']}"\n        )\n    if po_odeljenju["IV2"] != 4:\n        izvestaj.greske.append(\n            f"NP-сала: IV2 мора имати 4 блока (два двочаса), има {po_odeljenju['IV2']}"\n        )\n    treci = po_odeljenju["III1"] + po_odeljenju["III2"]\n    if treci != 2:\n        izvestaj.greske.append(\n            f"NP-сала: III1 или III2 морају имати један двочас (2 блока), има {treci}"\n        )\n    if len(np_casovi) != 10:\n        izvestaj.greske.append(\n            f"NP-сала: потребно је укупно 10 блокова (5 двочаса), има {len(np_casovi)}"\n        )\n\n\n'''
          if marker_func not in t:
              raise SystemExit('Nije pronadjeno mesto za NP validator')
          t = t.replace(marker_func, nova_func + marker_func, 1)
          p.write_text(t, encoding='utf-8')

          test = Path('tests/test_pravila_resursa.py')
          test.write_text('''from pathlib import Path\n\nfrom src.resavac import NP_SALA, _moguce_prostorije, ucitaj_standardne_ulaze\n\n\ndef _zahtev(ulaz, predmet, odeljenje):\n    return next(z for z in ulaz.zahtevi if z.predmet == predmet and odeljenje in z.odeljenja)\n\n\ndef test_np_sala_samo_za_propisane_rkb_grupe():\n    ulaz, prostorije, _ = ucitaj_standardne_ulaze(Path("ulazi"))\n    iv1 = _zahtev(ulaz, "Репертоар класичног балета", "IV1")\n    i1 = _zahtev(ulaz, "Репертоар класичног балета", "I1")\n    assert NP_SALA in {p.oznaka for p in _moguce_prostorije(iv1, ulaz, prostorije)}\n    assert NP_SALA not in {p.oznaka for p in _moguce_prostorije(i1, ulaz, prostorije)}\n''', encoding='utf-8')
          PY
      - name: Pokreni testove
        run: |
          python -m pip install -r requirements.txt
          python -m pytest -q
      - name: Commituj izmene
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/resavac.py src/proveravac.py tests/test_pravila_resursa.py
          git commit -m "Primeni pravila korepetitora i Narodnog pozorista"
          git push origin HEAD:sync-google-sheet-2026-08-25
