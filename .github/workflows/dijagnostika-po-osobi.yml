name: Dijagnostika jedinstvenog resursa po osobi

on:
  push:
    paths:
      - '.github/workflows/dijagnostika-po-osobi.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  osoba:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    strategy:
      fail-fast: false
      matrix:
        osoba:
          - 'Ђорђина Убовић'
          - 'Јелена Михаиловић Красић'
          - 'Марија Цветковић'
          - 'Соња Пана Виријевић'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Dodaj jedinstven resurs samo za osobu
        env:
          OSOBA: ${{ matrix.osoba }}
        run: |
          python - <<'PY'
          import os
          from pathlib import Path
          p = Path('src/resavac.py')
          t = p.read_text(encoding='utf-8')
          needle = '''    for intervali in intervali_korepetitora.values():\n        model.add_no_overlap(intervali)\n    for intervali in intervali_prostorija.values():\n'''
          repl = '''    for intervali in intervali_korepetitora.values():\n        model.add_no_overlap(intervali)\n    osoba_dijagnostika = ''' + repr(os.environ['OSOBA']) + '''\n    model.add_no_overlap(\n        intervali_nastavnika.get(osoba_dijagnostika, [])\n        + intervali_korepetitora.get(osoba_dijagnostika, [])\n    )\n    if sa_nedeljom_b:\n        model.add_no_overlap(\n            intervali_nastavnika_b.get(osoba_dijagnostika, [])\n            + intervali_korepetitora_b.get(osoba_dijagnostika, [])\n        )\n    for intervali in intervali_prostorija.values():\n'''
          if needle not in t:
              raise SystemExit('Nije pronadjen blok resursa')
          p.write_text(t.replace(needle, repl, 1), encoding='utf-8')
          PY
      - name: Instaliraj
        run: python -m pip install -r requirements.txt
      - name: Proveri jednu nedelju
        env:
          OSOBA: ${{ matrix.osoba }}
        run: |
          python - <<'PY'
          import os
          from ortools.sat.python import cp_model
          from src.model import Smena
          from src.resavac import napravi_model, ucitaj_standardne_ulaze
          ulaz, prostorije, nedostupnosti = ucitaj_standardne_ulaze('ulazi')
          model, _, _ = napravi_model(ulaz, prostorije, nedostupnosti, Smena.CRVENA, (), sa_nedeljom_b=False)
          model.clear_objective()
          solver = cp_model.CpSolver()
          solver.parameters.max_time_in_seconds = 180
          solver.parameters.num_search_workers = 8
          solver.parameters.random_seed = 1
          status = solver.solve(model)
          print(f"OSOBA={os.environ['OSOBA']}; STATUS={solver.status_name(status)}; WALL={solver.wall_time:.2f}s")
          if status == cp_model.INFEASIBLE:
              raise SystemExit(2)
          if status not in (cp_model.OPTIMAL, cp_model.FEASIBLE):
              raise SystemExit(3)
          PY
